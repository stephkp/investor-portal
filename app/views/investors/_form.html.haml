= form_with(model: investor, local: true, html: { class: 'form', novalidate: true }) do |form|
  - if investor.errors.any?
    .alert.alert--error{role: "alert"}
      %div
        = pluralize(investor.errors.count, "error")
        prohibited this investor from being saved:
      %ul.alert__list
        - investor.errors.full_messages.each do |message|
          %li= message

  .form__grid.form__grid--2
    .form__field
      = form.label :first_name, class: 'form__label'
      = form.text_field :first_name, class: 'form__input', required: true
    .form__field
      = form.label :last_name, class: 'form__label'
      = form.text_field :last_name, class: 'form__input', required: true

  .form__grid.form__grid--2
    .form__field
      = form.label :date_of_birth, 'Date of Birth', class: 'form__label'
      = form.date_field :date_of_birth, class: 'form__input', required: true, max: Date.today.years_ago(18).to_s
    .form__field
      = form.label :ssn, 'Social Security Number', class: 'form__label'
      = form.text_field :ssn, class: 'form__input', required: true, pattern: '\d{3}-\d{2}-\d{4}', placeholder: '123-45-6789', maxlength: 11, inputmode: 'numeric', data: { ssn_mask: true }

  .form__grid.form__grid--2
    .form__field
      = form.label :phone_number, 'Phone Number', class: 'form__label'
      = form.telephone_field :phone_number, class: 'form__input', required: true, pattern: '\d{10}|(?:\d{3}-){2}\d{4}|\(\d{3}\)\d{3}-?\d{4}'
    .form__field
      = form.label :street_address, 'Street Address', class: 'form__label'
      = form.text_field :street_address, class: 'form__input', required: true

  .form__grid.form__grid--3
    .form__field
      = form.label :city, class: 'form__label'
      = form.text_field :city, class: 'form__input', required: true
    .form__field
      = form.label :state, class: 'form__label'
      = form.select :state, options_for_select(%w[AL AK AZ AR CA CO CT DE FL GA HI ID IL IN IA KS KY LA ME MD MA MI MN MS MO MT NE NV NH NJ NM NY NC ND OH OK OR PA RI SC SD TN TX UT VT VA WA WV WI WY], investor.state), { include_blank: 'Select a state' }, class: 'form__input form__select', required: true
    .form__field
      = form.label :zip_code, 'Zip Code', class: 'form__label'
      = form.text_field :zip_code, class: 'form__input', required: true, pattern: '\d{5}(-\d{4})?'

  .form__field
    = form.label :documents, 'Documents', class: 'form__label'
    = form.file_field :documents, multiple: true, class: 'form__input form__input--documents', required: false

  .form__actions
    = form.submit 'Save Changes', class: 'btn btn--primary'
    = link_to 'Cancel', investors_path, class: 'btn btn--secondary'